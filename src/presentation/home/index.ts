/**
 * Feature 018 — Home presentation barrel.
 *
 * Pure, deterministic, framework-agnostic selectors/constants consumed by the
 * `useHome` hook and the Home React components. No React, Dexie, Worker,
 * engine, network, statistics computation or Recharts import.
 */

export * from './constants';
export * from './continue';
export * from './partition';
export * from './primaryAction';
